node_modules/
*.log

# Local overrides and secrets — never commit
.secret.local
.secret
.env.local
.env.*.local

# .env and .env.example are tracked (non-sensitive shared config / template only)

# Generated local email HTML previews (see scripts/preview-emails.js)
email-previews/
